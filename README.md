# Advent of Code 2025

Solutions are run using the `aoc-runner` fish shell script, and implemented mostly in Rust.

Input files are located in the `input/` folder and named `dayXX.txt`, where `XX` is the zero-padded day number.
